{
  "installCommand": "rm -f package-lock.json frontend/package-lock.json backend/package-lock.json && npm install && npx prisma db push --schema=backend/prisma/schema.prisma --skip-generate",
  "buildCommand": "npm --prefix frontend run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!.*\\..*).*)", "destination": "/index.html" }
  ]
}


